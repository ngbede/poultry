enum FieldType {
  firstName,
  lastName,
  email,
  password,
  phone,
  farmName,
  numberOfLayers,
  numberOfBroilers,
  address,
  state,
  lga,
  yearEstablished,
}

enum UserType { farmer, distributor }
